import DashboardLayout from "@/layout/dashboard.layout";
import React from "react";

type Props = {};

export default function Pesanan({}: Props) {
  return <DashboardLayout title="Pesanan">Pesanan</DashboardLayout>;
}
